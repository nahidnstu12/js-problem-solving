function sortByLength(arr) {

	// const newarr = arr.sort((a,b)=> a.length - b.length)
    return arr.sort((a,b)=> a.length - b.length)
}

// console.log( sortByLength(["apple", "pie", "p","","shortcake",'a', 'bb', 'ccc', 'dddd','xyz']))
console.log(["may", "april", "september", "august"])

// output   [ 'may', 'april', 'september', 'august' ]